# ── Core Inference ──────────────────────────────────────────────────────────
ultralytics>=8.2.0          # YOLOv8 — detection + segmentation
supervision>=0.21.0         # ByteTrack wrapper + annotation utilities

# ── PLC / Industrial ────────────────────────────────────────────────────────
pymodbus>=3.6.0             # Modbus TCP client

# ── Database ────────────────────────────────────────────────────────────────
pyodbc>=5.1.0               # MS SQL Server via ODBC 18

# ── Web / Streaming ─────────────────────────────────────────────────────────
Flask>=3.0.0
Werkzeug>=3.0.0

# ── Data & Utilities ────────────────────────────────────────────────────────
numpy>=1.26.0
Pillow>=10.3.0
scipy>=1.13.0
pandas>=2.2.0
matplotlib>=3.8.0
PyYAML>=6.0
tqdm>=4.66.0
psutil>=5.9.0

# ── Note ────────────────────────────────────────────────────────────────────
# lap>=0.5.12       is installed directly in the Dockerfile (Stage 6)
# PyQt6>=6.4.0      is installed directly in the Dockerfile (Stage 6)
# PyQt6-Qt6>=6.4.0  is installed directly in the Dockerfile (Stage 6)
# opencv-python     is NOT needed — OpenCV is compiled from source with CUDA
